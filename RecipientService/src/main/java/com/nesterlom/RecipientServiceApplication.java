package com.nesterlom;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class RecipientServiceApplication {
    public static void main(String[] args) {
        SpringApplication.run(RecipientServiceApplication.class, args);
    }
}

//todo mysql database
//todo csv to database


//todo check csv methods(write test, handle tests, add some features) - x

//todo handle template sending test

//todo sending email to some exact user - x

//todo get into API, integration and unit test(why we need each of them) and write some of that - x

